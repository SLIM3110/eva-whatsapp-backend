[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "python311Packages.setuptools"]

[phases.install]
cmds = ["npm ci --omit=dev", "pip install -r requirements.txt"]

[start]
cmd = "node src/server.js"
